// 03.Solicitar al usuario que ingrese una palabra y mostrar con qué letra comienza.

let palabra = prompt ("Ingrese una palabra: ")

let letra = palabra.charAt(0);

alert ("La primera letra de la palabra es: " + letra)